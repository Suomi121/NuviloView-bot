# Git state inventory — 2026-08-21

This inventory records the read-only audit performed before release-readiness work. It does not authorize force-push, destructive reset, or deletion of untracked user files.

## Remote state

- Repository: `Suomi121/NuviloView-bot`
- Default branch: `main`
- GitHub `main` at audit time: `38f2125` (`/setroll` PR #1 merged)
- Open pull requests at audit time: none
- Merged pull requests: PR #1, `Add safe reaction roles`
- `feat/security-v1` exists remotely but is not merged into `main`

## Local state before the release branch

- Local `main`: `9b71514`, two commits ahead of the former common base and two commits behind current GitHub `main`
- Security branch: `cb7ffe2`, containing the local Windows Bot control commits plus Security v1
- Stash: empty
- Worktrees: the primary Security worktree and the existing reaction-role publication worktree
- Safety branches created during the earlier repository recovery remain intact

Release-readiness work continues on `feat/release-readiness-20260821`. The first isolated commit is `8323298` (`fix: harden backup pipeline`).

## Working-tree classification

### A — retain and release deliberately

- Security v1 implementation and migrations
- Distributed singleton, host heartbeat, external monitor, and Developer Runtime panel
- ScopeServer removal
- Existing Guild Reset safety/UI refinements
- Spam/Snipe retention and moderation changes already requested by the operator
- Backup v2 safety work
- Documentation, privacy wording, monitoring, CI, migration inventory, and runbooks required for release readiness

### B — already integrated elsewhere

- `/setroll` and reaction-role migration are already merged into GitHub `main` by PR #1
- The untracked reaction-role utility and migration files in the primary worktree match GitHub `main` byte-for-byte
- Reaction-role test copies contain formatting-only drift and must not be published as a duplicate feature commit

### C — generated or host-local; preserve but do not commit

- `output/` audit PDF/HTML/render artifacts (now ignored)
- `backups/`, `logs/`, `data/`, `tools/`, `.next/`, and host runtime files
- Existing F:/G: backup generations and legacy local staging data

### D — obsolete by explicit product decision

- ScopeServer utility and its dedicated test are deleted locally
- Dead imports, commands, routes, navigation, help text, and documentation references must remain absent before release
- The historical mention in `docs/nuke-protection.md` is explanatory only and may be reworded to avoid presenting ScopeServer as a product feature

### E — verify before release

- Environment flags in each host and Vercel
- Production external-monitor destination and ownership
- Real-host singleton failover evidence
- Health v2 calibration and data-quality evidence
- Guild Reset release decision; the default remains disabled

## Safe integration rule

Do not recommit reaction-role implementation files. Validate the final combined tree against current `origin/main` in an isolated merge/build check, then publish release-readiness changes through a pull request. Do not force-push or publish a GitHub Release without a separate explicit decision.
